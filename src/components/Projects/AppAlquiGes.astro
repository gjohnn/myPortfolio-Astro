---
// AlquiGes - Rental Management System
---

<section class="card p-6 sm:p-8 relative overflow-hidden">
  <!-- Comic Panel Header -->
  <div class="flex items-center gap-4 mb-6">
    <div class="w-16 h-16 rounded-lg flex items-center justify-center flex-shrink-0"
         style="background: var(--comic-orange); border: 3px solid var(--comic-black); box-shadow: 4px 4px 0 var(--comic-black);">
      <i class="fas fa-warehouse text-white text-2xl"></i>
    </div>
    <div>
      <h3 class="comic-title text-2xl" style="color: var(--comic-orange);">AlquiGes</h3>
      <p style="font-family: var(--font-body); color: var(--gray-600); font-weight: 700;">Rental Management System</p>
    </div>
  </div>

  <!-- Description -->
  <div class="speech-bubble mb-6">
    <p style="font-family: var(--font-body); color: var(--comic-black); font-weight: 700;">
      Full-stack rental management system with client tracking, inventory control, and financial reporting.
    </p>
  </div>

  <!-- Tech Grid -->
  <div class="grid grid-cols-3 gap-3 mb-6">
    <div class="card p-3 text-center" style="border-color: var(--comic-blue);">
      <i class="fab fa-js text-2xl mb-1" style="color: var(--comic-blue);"></i>
      <p class="text-xs font-bold" style="font-family: var(--font-body); color: var(--comic-black);">TypeScript</p>
    </div>
    <div class="card p-3 text-center" style="border-color: var(--comic-green);">
      <i class="fas fa-database text-2xl mb-1" style="color: var(--comic-green);"></i>
      <p class="text-xs font-bold" style="font-family: var(--font-body); color: var(--comic-black);">Supabase</p>
    </div>
    <div class="card p-3 text-center" style="border-color: var(--comic-purple);">
      <i class="fab fa-react text-2xl mb-1" style="color: var(--comic-purple);"></i>
      <p class="text-xs font-bold" style="font-family: var(--font-body); color: var(--comic-black);">Next.js</p>
    </div>
  </div>

  <!-- Features -->
  <div class="mb-6">
    <h4 class="text-sm mb-3" style="font-family: var(--font-accent); color: var(--comic-red); letter-spacing: 1px;">KEY FEATURES</h4>
    <div class="space-y-2">
      <div class="flex items-center gap-2">
        <i class="fas fa-check-circle" style="color: var(--comic-green);"></i>
        <span style="font-family: var(--font-body); font-weight: 700; color: var(--comic-black);">Client Management</span>
      </div>
      <div class="flex items-center gap-2">
        <i class="fas fa-check-circle" style="color: var(--comic-green);"></i>
        <span style="font-family: var(--font-body); font-weight: 700; color: var(--comic-black);">Inventory Control</span>
      </div>
      <div class="flex items-center gap-2">
        <i class="fas fa-check-circle" style="color: var(--comic-green);"></i>
        <span style="font-family: var(--font-body); font-weight: 700; color: var(--comic-black);">Financial Reports</span>
      </div>
    </div>
  </div>

  <!-- CTA -->
  <a href="https://frontend-alqui-ges.vercel.app" target="_blank" rel="noopener noreferrer"
     class="btn btn-primary w-full justify-center">
    <i class="fas fa-external-link-alt mr-2"></i>
    Visit Site
  </a>
</section>