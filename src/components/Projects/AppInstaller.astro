---
import "../../styles/global.css";
---

<article class="card card-hover p-8 mb-8 animate-on-scroll">
  <!-- Project Header -->
  <div class="flex flex-col lg:flex-row gap-8 items-start">
    <!-- Project Info -->
    <div class="flex-1">
      <div class="flex items-center gap-4 mb-4">
        <div class="w-12 h-12 bg-gradient-to-br from-green-500 to-green-600 rounded-xl flex items-center justify-center">
          <i class="fas fa-download text-white text-xl"></i>
        </div>
        <div>
          <h3 class="text-2xl font-bold text-white mb-1">My App Installer</h3>
          <p class="text-gray-400 text-sm">Automated Windows Application Installer</p>
        </div>
      </div>

      <!-- Description -->
      <p class="text-gray-300 mb-6 leading-relaxed">
        A powerful PowerShell script that automates the installation of essential Windows applications 
        using Winget. Streamlines the setup process for new Windows 10/11 systems with a curated 
        selection of developer tools and applications.
      </p>

      <!-- Technologies -->
      <div class="mb-6">
        <h4 class="text-sm font-semibold text-gray-400 uppercase tracking-wider mb-3">Technologies Used</h4>
        <div class="flex flex-wrap gap-2">
          <span class="px-3 py-1 bg-blue-500/20 text-blue-400 rounded-lg border border-blue-500/30 text-sm font-medium">
            <i class="fab fa-microsoft mr-1"></i>
            PowerShell 7
          </span>
          <span class="px-3 py-1 bg-cyan-500/20 text-cyan-400 rounded-lg border border-cyan-500/30 text-sm font-medium">
            <i class="fas fa-package mr-1"></i>
            Winget
          </span>
          <span class="px-3 py-1 bg-blue-600/20 text-blue-300 rounded-lg border border-blue-600/30 text-sm font-medium">
            <i class="fab fa-windows mr-1"></i>
            Windows 10/11
          </span>
        </div>
      </div>

      <!-- Key Features -->
      <div class="mb-6">
        <h4 class="text-sm font-semibold text-gray-400 uppercase tracking-wider mb-3">Key Features</h4>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
          <div class="flex items-center gap-2 text-gray-300">
            <i class="fas fa-check-circle text-green-400 text-sm"></i>
            <span class="text-sm">Automated batch installation</span>
          </div>
          <div class="flex items-center gap-2 text-gray-300">
            <i class="fas fa-check-circle text-green-400 text-sm"></i>
            <span class="text-sm">Error handling & logging</span>
          </div>
          <div class="flex items-center gap-2 text-gray-300">
            <i class="fas fa-check-circle text-green-400 text-sm"></i>
            <span class="text-sm">Customizable app lists</span>
          </div>
          <div class="flex items-center gap-2 text-gray-300">
            <i class="fas fa-check-circle text-green-400 text-sm"></i>
            <span class="text-sm">System requirements check</span>
          </div>
        </div>
      </div>

      <!-- Action Buttons -->
      <div class="flex flex-col sm:flex-row gap-3">
        <a 
          href="https://github.com/gjohnn/my-app-installer" 
          target="_blank"
          rel="noopener noreferrer"
          class="btn btn-primary group flex-1 sm:flex-none"
        >
          <i class="fab fa-github mr-2 group-hover:animate-bounce"></i>
          View on GitHub
        </a>
        <button 
          id="copyScriptBtn"
          class="btn btn-secondary group flex-1 sm:flex-none"
          onclick="copyInstallCommand()"
        >
          <i id="copyIcon" class="fas fa-copy mr-2 group-hover:animate-bounce"></i>
          <span id="copyText">Copy Install Command</span>
        </button>
      </div>
    </div>

    <!-- Visual Element -->
    <div class="w-full lg:w-80 flex-shrink-0">
      <div class="relative">
        <!-- Terminal Window -->
        <div class="bg-gray-900 rounded-lg border border-gray-700 overflow-hidden">
          <!-- Terminal Header -->
          <div class="bg-gray-800 px-4 py-2 flex items-center gap-2 border-b border-gray-700">
            <div class="flex gap-1">
              <div class="w-3 h-3 bg-red-500 rounded-full"></div>
              <div class="w-3 h-3 bg-yellow-500 rounded-full"></div>
              <div class="w-3 h-3 bg-green-500 rounded-full"></div>
            </div>
            <span class="text-gray-400 text-sm ml-2">PowerShell</span>
          </div>
          
          <!-- Terminal Content -->
          <div class="p-4 font-mono text-sm">
            <div class="text-green-400 mb-2">PS C:\&gt; .\install-apps.ps1</div>
            <div class="text-gray-300 mb-1">🔍 Checking system requirements...</div>
            <div class="text-blue-400 mb-1">✅ Winget found and ready</div>
            <div class="text-yellow-400 mb-1">📦 Installing applications...</div>
            <div class="text-green-400">✨ Installation complete!</div>
          </div>
        </div>

        <!-- Floating Icons -->
        <div class="absolute -top-4 -right-4 w-8 h-8 bg-blue-500/20 rounded-lg flex items-center justify-center animate-float">
          <i class="fab fa-microsoft text-blue-400"></i>
        </div>
        <div class="absolute -bottom-4 -left-4 w-8 h-8 bg-green-500/20 rounded-lg flex items-center justify-center animate-float" style="animation-delay: -1s;">
          <i class="fas fa-terminal text-green-400"></i>
        </div>
      </div>
    </div>
  </div>

  <!-- Hidden content for copying -->
  <textarea 
    id="installCommand" 
    class="sr-only" 
    readonly
  >iwr -useb https://raw.githubusercontent.com/gjohnn/my-app-installer/main/install.ps1 | iex</textarea>
</article>

<script is:inline>
  function copyInstallCommand() {
    const command = document.getElementById('installCommand').value;
    const btn = document.getElementById('copyScriptBtn');
    const icon = document.getElementById('copyIcon');
    const text = document.getElementById('copyText');
    
    navigator.clipboard.writeText(command).then(() => {
      // Success feedback
      icon.className = 'fas fa-check mr-2 text-green-400';
      text.textContent = 'Copied!';
      btn.classList.add('bg-green-600/20', 'border-green-500/50', 'text-green-400');
      btn.classList.remove('bg-gray-700', 'border-gray-600');
      
      // Reset after 3 seconds
      setTimeout(() => {
        icon.className = 'fas fa-copy mr-2 group-hover:animate-bounce';
        text.textContent = 'Copy Install Command';
        btn.classList.remove('bg-green-600/20', 'border-green-500/50', 'text-green-400');
        btn.classList.add('bg-gray-700', 'border-gray-600');
      }, 3000);
    }).catch(() => {
      // Error feedback
      icon.className = 'fas fa-exclamation-triangle mr-2 text-red-400';
      text.textContent = 'Copy failed';
      
      // Reset after 2 seconds
      setTimeout(() => {
        icon.className = 'fas fa-copy mr-2 group-hover:animate-bounce';
        text.textContent = 'Copy Install Command';
      }, 2000);
    });
  }
</script>
